PORT=4002
# CHANGE THIS: same Neon connection string as auth-service (or a separate DB, see README)
DATABASE_URL=postgres://username:password@ep-xxxx.neon.tech/neondb?sslmode=require
# CHANGE THIS: MUST be identical to auth-service's JWT_SECRET (so tokens issued by auth-service verify here)
JWT_SECRET=change_this_to_a_long_random_secret
